@model  IEnumerable<Stregelisten.Data.Beverage>

@{
    ViewData["Title"] = "Selection";
    Layout = "~/Views/Shared/_Layout.cshtml";    
}

<h2>Drinks Menu</h2>

<div class="stock">
    <table class="table selectiontable">
        @*<thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Price)
                </th>                
            </tr>
        </thead>*@
        <tbody>
            @foreach (var item in Model)
            {
            <tr>
                <td>                    
                    <button class="@item.Id" id="@item.Id"  value="@item.Name">@Html.DisplayFor(modelItem => item.Name)</button>                
                </td>
                <td>
                    <label class="@item.Id" title="@item.Price">@Html.DisplayFor(modelItem => item.Price)</label>
                                      
                </td>               
            </tr>
            }
        </tbody>
    </table>
</div>

<div style="margin-top: 20px;">
    <div class="row">
        <div class="col-sm-8 col-sm-offset-2">
            <div class="tab"></div>
        </div>
    </div>
    <div class="row receipt hidden">
        <div class="col-sm-6 col-sm-offset-3 container-print">
            <div class="panel panel-dark panel-default pseudo">
                <div class="panel-body">
                    <div class="row">
                        <div class="col-xs-8"><b>Drinks</b></div>
                        <div class="col-xs-4"><b>Pris</b></div>
                    </div>
                    
                    <hr class="barhr" />
                    <div class="row">
                        <div class="col-xs-8" id="productCount"><b>Total</b></div>
                        <div class="col-xs-4" id="total"></div>
                    </div>
                    <div class="row text-center">
                        <button class="btn btn-success" id="submitButton">
                            Godkend
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>  








